var cipherMat = process.env.CIPHER_MATRIX;
		 //Darth, if you somehow make this matrix private, make sure to compeltely re-randomise the matrix.
module.exports = {
  name: "default",
  usage: "cipher <encode|decode> <message>",
  desc: "Encodes a message or decodes an encrypted message that was encoded in the bot",
  alias: ["code"],
  run (client, message, args) {

  }
}